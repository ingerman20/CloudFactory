import { StyleSheet } from 'react-native';

export const tableStyles = StyleSheet.create({
  headCell: {
    flex: 1,
    flexDirection: 'row',
    textAlign: 'left',
    width: 200,
    fontWeight: 'bold',
  },
});
